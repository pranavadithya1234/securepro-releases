[build]
  publish = "website-deploy"   # Netlify deploys this folder
  command = "echo 'Static site — no build needed'"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
